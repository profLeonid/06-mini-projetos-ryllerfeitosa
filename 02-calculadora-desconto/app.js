const calcularDesconto = function() {
    const precoOriginal = document.getElementById('precoOriginal')
    const desconto      = document.getElementById('desconto')

    const valorFinal = precoOriginal.value - desconto.value
}